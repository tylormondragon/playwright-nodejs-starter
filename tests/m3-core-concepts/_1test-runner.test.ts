import { test } from '@playwright/test';   
/**
 * Tests in the same file must have different titles,
 * tests in different files can have the same title.
 */

test('Test title 1', async ({ page }) => {

});

test('Test title 2', async ({ page }) => { 

}); 